namespace StorageMongoDB.Data
{
    public class StorageMongoContext<T>
    {
        /*
                private readonly IMongoDatabase _mongoDatabase;
                public readonly IMongoCollection<T> MongoCollection;
        
                public StorageMongoContext(IMongoDatabase mongoDatabase)
                {
                    _mongoDatabase = mongoDatabase;
                    MongoCollection = _mongoDatabase.GetCollection<T>($"{T}");
                }
         */
    }
}
